name: Tag

on:
  workflow_dispatch: 
    inputs:
      version:
        description: 'Release version (e.g. v0.1.0)'
        required: true
      message:
        description: 'Tag message'
        required: true

jobs:
  tag:
    uses: upbound/official-providers-ci/.github/workflows/provider-tag.yml@44c3fe875827c37d9621c0b9221052313738fd6f #v0.12.0
    with:
      version: ${{ github.event.inputs.version }}
      message: ${{ github.event.inputs.message }}
